import React,{useEffect,useState} from 'react';
import {Activity,ArrowRight,BarChart3,Bell,Check,Flame,Home as HomeIcon,LogOut,Menu,Plus,Sparkles,Target,Trophy,User,X,Zap} from 'lucide-react';
import {supabase} from './lib/supabase';
import {useNavigation} from './app/AppShell.jsx';
import './lifepilot-v2.css';

const today=()=>new Date().toISOString().slice(0,10);
const dateLabel=d=>new Date(`${d}T12:00:00`).toLocaleDateString('it-IT',{weekday:'long',day:'numeric',month:'long'});
const pct=(a,b)=>b?Math.round(a/b*100):0;
const QUIZ=[
 {skill:'Concentrazione',q:'Qual è il modo migliore per iniziare una sessione di lavoro profondo?',a:['Aprire le notifiche','Scegliere una sola attività e ridurre le distrazioni','Cambiare attività spesso','Controllare i social'],c:1},
 {skill:'Pianificazione',q:'Qual è il primo passo utile per un obiettivo grande?',a:['Lasciarlo generico','Dividerlo in azioni concrete e misurabili','Aspettare la motivazione','Fare tutto in un giorno'],c:1},
 {skill:'Costanza',q:'Cosa aiuta a costruire un’abitudine?',a:['Perfezione','Aumentare sempre la difficoltà','Ripetere piccoli comportamenti con regolarità','Saltare i giorni difficili'],c:2},
 {skill:'Priorità',q:'Quando hai molte attività, quale viene prima?',a:['Quella con maggior impatto sull’obiettivo','Quella più facile','Tutte insieme','Quella più rumorosa'],c:0},
 {skill:'Autovalutazione',q:'A cosa serve misurare le proprie abilità?',a:['A giudicarsi','A confrontarsi','A capire dove migliorare e adattare il percorso','A evitare la pratica'],c:2}
];

export default function LifePilotCore(){
 const {route,navigate}=useNavigation();
 const [session,setSession]=useState(null),[boot,setBoot]=useState(true),[profile,setProfile]=useState(null),[goals,setGoals]=useState([]),[tasks,setTasks]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[drawer,setDrawer]=useState(false);
 const uid=session?.user?.id;
 const load=async()=>{if(!uid)return;setLoading(true);setError('');try{const [p,g,t]=await Promise.all([supabase.from('profiles').select('*').eq('id',uid).maybeSingle(),supabase.from('goals').select('*').eq('user_id',uid).order('created_at',{ascending:false}),supabase.from('goal_tasks').select('*').eq('user_id',uid).order('task_date')]);const bad=[p,g,t].find(x=>x.error)?.error;if(bad)throw bad;setProfile(p.data);setGoals(g.data||[]);setTasks(t.data||[])}catch(e){setError(e.message||'Impossibile caricare i dati.')}finally{setLoading(false)}};
 useEffect(()=>{let alive=true;supabase.auth.getSession().then(({data})=>{if(alive){setSession(data.session);setBoot(false)}});const {data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>{alive=false;subscription.unsubscribe()}},[]);
 useEffect(()=>{if(uid)load()},[uid]);
 if(boot)return <Splash/>;
 if(!session)return <Auth/>;
 if(loading&&!profile)return <Splash text="Carichiamo il tuo percorso…"/>;
 if(error&&!profile)return <Centered><div className="card setup"><h1>Riproviamo.</h1><p className="muted">{error}</p><button className="primary wide" onClick={load}>Ricarica<ArrowRight/></button></div></Centered>;
 if(!profile)return null;
 const nav=v=>{navigate(v);setDrawer(false)};
 const logout=()=>supabase.auth.signOut();
 return <div className="lp-app"><header className="lp-top"><button className="icon" onClick={()=>setDrawer(true)} aria-label="Apri menu"><Menu/></button><button className="lp-brand" onClick={()=>nav('home')}><span><Sparkles/></span>LifePilot</button><div className="top-actions"><button className="avatar" onClick={()=>nav('profile')}>{(profile.nickname||'U')[0].toUpperCase()}</button></div></header><aside className="lp-side"><Nav route={route} nav={nav}/><button className="logout" onClick={logout}><LogOut/>Esci</button></aside>{drawer&&<div className="drawer-bg" onClick={()=>setDrawer(false)}><aside className="lp-drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><button className="lp-brand" onClick={()=>nav('home')}><span><Sparkles/></span>LifePilot</button><button className="icon" onClick={()=>setDrawer(false)} aria-label="Chiudi"><X/></button></div><Nav route={route} nav={nav}/><button className="logout" onClick={logout}><LogOut/>Esci</button></aside></div>}<main className="lp-main">{route==='home'&&<Dashboard profile={profile} goals={goals} tasks={tasks} nav={nav}/>} {route==='plan'&&<Plan goals={goals} tasks={tasks} uid={uid} reload={load}/>} {route==='goals'&&<Goals goals={goals} tasks={tasks} uid={uid} reload={load}/>} {route==='focus'&&<Focus goals={goals} uid={uid} nav={nav}/>} {route==='gameplay'&&<Gameplay goals={goals} tasks={tasks} profile={profile} nav={nav}/>} {route==='quiz'&&<Quiz goals={goals} uid={uid} nav={nav}/>} {route==='progress'&&<Progress goals={goals} tasks={tasks} profile={profile}/>} {route==='notifications'&&<Notifications uid={uid}/>} {route==='profile'&&<Profile profile={profile} uid={uid} reload={load}/>}</main></div>;
}
function Splash({text='Prepariamo il tuo percorso…'}){return <div className="lp-splash"><div className="lp-logo"><Sparkles/></div><h1>LifePilot</h1><p>{text}</p></div>}
function Auth(){const [mode,setMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState(''),[ok,setOk]=useState('');const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{if(password.length<6)throw Error('La password deve contenere almeno 6 caratteri.');const r=mode==='signup'?await supabase.auth.signUp({email:email.trim().toLowerCase(),password}):await supabase.auth.signInWithPassword({email:email.trim().toLowerCase(),password});if(r.error)throw r.error;if(mode==='signup'&&!r.data.session)setOk('Account creato. Controlla la tua email.')}catch(e){setError(e.message||'Accesso non riuscito.')}finally{setBusy(false)}};return <div className="lp-auth"><div className="lp-auth-card"><div className="lp-logo small"><Sparkles/></div><span className="eyebrow">LIFEPILOT</span><h1>{mode==='login'?'Bentornato.':'Iniziamo.'}</h1><p className="muted">Trasforma un obiettivo in un percorso quotidiano.</p><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label>{error&&<div className="msg error">{error}</div>}{ok&&<div className="msg ok">{ok}</div>}<button className="primary wide" disabled={busy}>{busy?'Attendi…':mode==='login'?'Accedi':'Crea account'}<ArrowRight/></button></form><button className="link-btn" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Non hai un account? Registrati':'Hai già un account? Accedi'}</button></div></div>}
function Nav({route,nav}){return <nav className="nav">{[['home','Home',HomeIcon],['plan','Il mio piano',Sparkles],['goals','Obiettivi',Target],['focus','Focus',Zap],['gameplay','Gameplay',Trophy],['quiz','Quiz & Abilità',Sparkles],['progress','Progressi',BarChart3],['notifications','Notifiche',Bell],['profile','Profilo',User]].map(([v,l,I])=><button className={route===v?'active':''} onClick={()=>nav(v)} key={v}><I/>{l}</button>)}</nav>}
function Dashboard({profile,goals,tasks,nav}){const todayTasks=tasks.filter(t=>t.task_date===today()),done=todayTasks.filter(t=>t.completed).length,goal=goals[0];return <div className="page"><div className="hero"><div><span className="eyebrow">{dateLabel(today())}</span><h1>Cosa devo fare oggi?</h1><p>{profile.nickname}, il tuo prossimo passo è tutto ciò che serve per avanzare.</p></div><button className="primary" onClick={()=>nav('focus')}>Inizia Focus<Zap/></button></div><div className="metric-grid"><Metric icon={<Check/>} label="Oggi" value={`${done}/${todayTasks.length}`}/><Metric icon={<Target/>} label="Obiettivi" value={goals.length}/><Metric icon={<Activity/>} label="Azioni" value={tasks.filter(t=>t.completed).length}/><Metric icon={<Trophy/>} label="Gameplay" value="Attivo"/></div>{goal?<Panel><div className="panel-head"><div><span className="eyebrow">MISSIONE DI OGGI</span><h2>{todayTasks[0]?.title||goal.title}</h2><p className="muted">Obiettivo: {goal.title}</p></div><Target/></div>{todayTasks.length?todayTasks.slice(0,5).map(t=><TaskRow key={t.id} task={t}/>):<p className="muted">Nessuna missione programmata per oggi. Apri il Piano per crearla.</p>}<div className="quick-row"><button onClick={()=>nav('plan')}>Apri Piano</button><button className="primary" onClick={()=>nav('gameplay')}>Continua Gameplay<ArrowRight/></button></div></Panel>:<Empty title="Crea il tuo primo obiettivo" text="Il loop di LifePilot parte da una destinazione chiara." action="Crea obiettivo" onClick={()=>nav('goals')}/>}<Panel><div className="panel-head"><div><span className="eyebrow">LOOP</span><h2>Piano → Missione → Focus → Quiz → Ricompensa → Progresso</h2></div><Sparkles/></div><p className="muted">Ogni passaggio alimenta lo stesso percorso, senza workaround DOM.</p></Panel></div>}
function Plan({goals,tasks,uid,reload}){const [goalId,setGoalId]=useState(goals[0]?.id||''),[title,setTitle]=useState(goals[0]?.title||''),[days,setDays]=useState(String(goals[0]?.days||30)),[minutes,setMinutes]=useState(String(goals[0]?.daily_minutes||20)),[busy,setBusy]=useState(false),[error,setError]=useState('');const goal=goals.find(g=>g.id===goalId);useEffect(()=>{if(goal){setTitle(goal.title);setDays(String(goal.days||30));setMinutes(String(goal.daily_minutes||20))}},[goalId]);const generate=async()=>{if(!goal)return setError('Crea prima un obiettivo.');const d=Number(days),m=Number(minutes);if(!title.trim()||d<1||d>365||m<5||m>240)return setError('Controlla obiettivo, durata e minuti.');setBusy(true);setError('');try{const {data,error:e}=await supabase.functions.invoke('generate-plan',{body:{title:title.trim(),category:goal.category,days:d,dailyMinutes:m}});if(e)throw e;const items=Array.isArray(data?.plan)?data.plan:[];if(!items.length)throw Error(data?.error||'Il piano generato è vuoto.');const start=new Date();const rows=items.map((x,i)=>{const dt=new Date(start);dt.setDate(dt.getDate()+Math.max(0,Number(x.day||i+1)-1));return {goal_id:goal.id,user_id:uid,task_date:dt.toISOString().slice(0,10),title:String(x.title||`Azione ${i+1}`).trim(),priority:2}});await supabase.from('goal_tasks').delete().eq('goal_id',goal.id).eq('user_id',uid);const {error:ie}=await supabase.from('goal_tasks').insert(rows);if(ie)throw ie;const {error:ge}=await supabase.from('goals').update({title:title.trim(),days:d,daily_minutes:m,plan:items}).eq('id',goal.id).eq('user_id',uid);if(ge)throw ge;await reload()}catch(e){setError(e.message||'Impossibile generare il piano.')}finally{setBusy(false)}};return <div className="page"><Header title="Il mio piano" subtitle="Trasforma un obiettivo in azioni concrete."/>{!goals.length?<Empty title="Prima crea un obiettivo" text="Il piano nasce da una destinazione chiara." action="Crea obiettivo" onClick={()=>window.history.pushState({},'', '/app/goals')}/>:<><Panel><label>Obiettivo<select value={goalId} onChange={e=>setGoalId(e.target.value)}>{goals.map(g=><option key={g.id} value={g.id}>{g.title}</option>)}</select></label><div className="form-grid"><label>Cosa vuoi raggiungere?<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Durata (giorni)<input value={days} onChange={e=>setDays(e.target.value.replace(/\D/g,''))}/></label><label>Minuti al giorno<input value={minutes} onChange={e=>setMinutes(e.target.value.replace(/\D/g,''))}/></label></div>{error&&<div className="msg error">{error}</div>}<button className="primary" disabled={busy} onClick={generate}>{busy?'Generazione…':'Genera piano'}<Sparkles/></button></Panel><Panel><div className="panel-head"><h2>Prossime missioni</h2><Activity/></div>{tasks.filter(t=>t.goal_id===goalId&&!t.completed).slice(0,10).map(t=><TaskRow key={t.id} task={t}/>)}{!tasks.some(t=>t.goal_id===goalId&&!t.completed)&&<p className="muted">Nessuna missione ancora. Genera il piano.</p>}</Panel></>}</div>}
function Goals({goals,tasks,uid,reload}){const [open,setOpen]=useState(false),[title,setTitle]=useState(''),[cat,setCat]=useState('personale'),[error,setError]=useState('');const add=async e=>{e.preventDefault();if(!title.trim())return;const {error:e2}=await supabase.from('goals').insert({user_id:uid,title:title.trim(),category:cat,days:30,daily_minutes:20,priority:2,progress:0,streak:0});if(e2)setError(e2.message);else{setTitle('');setOpen(false);reload()}};const remove=async id=>{if(!confirm('Eliminare questo obiettivo?'))return;const {error:e}=await supabase.from('goals').delete().eq('id',id).eq('user_id',uid);if(e)setError(e.message);else reload()};return <div className="page"><Header title="Obiettivi" subtitle="Una destinazione chiara per il percorso." action={<button className="primary" onClick={()=>setOpen(true)}><Plus/>Nuovo</button>}/>{error&&<div className="msg error">{error}</div>}{open&&<Modal title="Nuovo obiettivo" close={()=>setOpen(false)}><form onSubmit={add}><label>Cosa vuoi raggiungere?<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} required/></label><label>Categoria<select value={cat} onChange={e=>setCat(e.target.value)}><option>carriera</option><option>salute</option><option>studio</option><option>finanze</option><option>personale</option></select></label><button className="primary wide">Crea obiettivo<ArrowRight/></button></form></Modal>}{goals.length?goals.map(g=>{const gt=tasks.filter(t=>t.goal_id===g.id),done=gt.filter(t=>t.completed).length;return <Panel key={g.id}><div className="goal-line"><div><span className="eyebrow">{g.category}</span><h2>{g.title}</h2><p className="muted">{done}/{gt.length} azioni · {g.days||30} giorni</p></div><button className="icon danger" onClick={()=>remove(g.id)} aria-label="Elimina"><X/></button></div><ProgressBar value={gt.length?pct(done,gt.length):0}/></Panel>}):<Empty title="Nessun obiettivo" text="Inizia con una cosa concreta." action="Crea obiettivo" onClick={()=>setOpen(true)}/>}</div>}
function Focus({goals,uid,nav}){const [minutes,setMinutes]=useState(25),[left,setLeft]=useState(1500),[running,setRunning]=useState(false),[sessionId,setSessionId]=useState(null),[done,setDone]=useState(false),[error,setError]=useState('');useEffect(()=>setLeft(minutes*60),[minutes]);useEffect(()=>{if(!running)return;const id=setInterval(()=>setLeft(v=>{if(v<=1){clearInterval(id);setRunning(false);setDone(true);return 0}return v-1}),1000);return()=>clearInterval(id)},[running]);const start=async()=>{const goal=goals[0];if(!goal)return setError('Crea prima un obiettivo.');const starts=new Date(),ends=new Date(starts.getTime()+minutes*60000);const {data,error:e}=await supabase.from('life_sessions').insert({user_id:uid,goal_id:goal.id,title:`Focus: ${goal.title}`,description:'Sessione Focus collegata al goal.',session_type:'focus',starts_at:starts.toISOString(),ends_at:ends.toISOString(),status:'active',xp_reward:25,coin_reward:10}).select('id').single();if(e){setError(e.message);return}setSessionId(data.id);setDone(false);setLeft(minutes*60);setRunning(true)};const finish=()=>{setRunning(false);setDone(true)};return <div className="page"><Header title="Focus" subtitle="Una sessione collegata al tuo obiettivo."/><Panel><div className="focus"><div className="timer">{String(Math.floor(left/60)).padStart(2,'0')}:{String(left%60).padStart(2,'0')}</div><div className="focus-presets">{[15,25,45,60].map(m=><button className={minutes===m?'selected':''} disabled={running} onClick={()=>setMinutes(m)} key={m}>{m} min</button>)}</div>{error&&<div className="msg error">{error}</div>}{done?<><div className="msg ok">Focus completato. Ora fai il quiz della sessione.</div><button className="primary" disabled={!sessionId} onClick={()=>nav(`quiz:${sessionId}`)}>Apri Quiz<ArrowRight/></button></>:<button className="primary" onClick={running?finish:start}>{running?'Concludi Focus':'Inizia Focus'}<Zap/></button>}</div></Panel></div>}
function Quiz({goals,uid,nav}){const {route}=useNavigation();const sessionId=route.startsWith('quiz:')?route.slice(5):null;const [index,setIndex]=useState(0),[answers,setAnswers]=useState([]),[busy,setBusy]=useState(false),[result,setResult]=useState(null),[error,setError]=useState('');const goal=goals[0];const submit=async()=>{if(!sessionId||!goal)return setError('Sessione o obiettivo mancanti.');const score=answers.reduce((n,a,i)=>n+(a===QUIZ[i].c?1:0),0);setBusy(true);setError('');try{const {data,error:e}=await supabase.rpc('record_quiz_result',{p_quiz_key:`session:${sessionId}`,p_goal_id:goal.id,p_session_id:sessionId,p_score:score,p_total:QUIZ.length,p_skills:QUIZ.map(q=>q.skill)});if(e)throw e;if(data?.passed){const {error:ce}=await supabase.rpc('complete_life_session',{p_session_id:sessionId});if(ce)throw ce}setResult({...data,score,total:QUIZ.length})}catch(e){setError(e.message||'Impossibile registrare il quiz.')}finally{setBusy(false)}};if(result)return <div className="page"><Panel><div className="lp-quiz-result"><span className="eyebrow">RISULTATO</span><h1>{result.score}/{result.total}</h1><div className="score">{result.passed?'✓':'↺'}</div><p>{result.passed?'Sessione completata. Ricompensa assegnata una sola volta.':'Quiz non superato. Riparti con una sessione più breve.'}</p>{result.passed&&<div className="lp-quiz-pill">+{result.xp} XP · +{result.lifepoints} LifePoints · +{result.lifecoins} LifeCoins</div>}<button className="primary" onClick={()=>nav('gameplay')}>Vai a Gameplay<ArrowRight/></button></div></Panel></div>;const q=QUIZ[index];return <div className="page"><Header title="Quiz della sessione" subtitle={`Domanda ${index+1} di ${QUIZ.length}`}/><Panel><span className="eyebrow">{q.skill}</span><h2>{q.q}</h2>{q.a.map((a,i)=><button className="task-button" key={a} onClick={()=>{const next=[...answers];next[index]=i;setAnswers(next)}}><span className={answers[index]===i?'check done':'check'}>{answers[index]===i&&<Check/>}</span><div><strong>{a}</strong></div></button>)}<div className="quick-row">{index>0&&<button onClick={()=>setIndex(index-1)}>Indietro</button>} {index<QUIZ.length-1?<button className="primary" disabled={answers[index]==null} onClick={()=>setIndex(index+1)}>Continua<ArrowRight/></button>:<button className="primary" disabled={answers.length!==QUIZ.length||busy} onClick={submit}>{busy?'Salvataggio…':'Concludi quiz'}<Check/></button>}</div>{error&&<div className="msg error">{error}</div>}</Panel></div>}
function Gameplay({goals,tasks,profile,nav}){const [skills,setSkills]=useState([]);useEffect(()=>{supabase.from('skill_progress').select('skill,mastery,attempts,correct').order('skill').then(({data})=>setSkills(data||[]))},[]);const done=tasks.filter(t=>t.completed).length;return <div className="page"><Header title="Gameplay" subtitle="Il vero loop: missione, Focus, quiz, ricompensa e progresso."/><section className="gameplay-hero"><div><span className="eyebrow">PLAYER</span><h2>{profile.nickname}</h2><p>Le ricompense arrivano dal server e sono idempotenti.</p></div><Trophy/></section><div className="metric-grid"><Metric icon={<Check/>} label="Task completati" value={done}/><Metric icon={<Flame/>} label="Streak" value={`${profile.current_streak||0} giorni`}/><Metric icon={<Sparkles/>} label="XP" value={profile.xp||0}/><Metric icon={<Trophy/>} label="LifeCoins" value={profile.lifecoins||0}/></div><Panel><div className="panel-head"><div><span className="eyebrow">OGGI</span><h2>Prossimo passo</h2></div><Target/></div><p className="muted">{tasks.find(t=>t.task_date===today()&&!t.completed)?.title||goals[0]?.title||'Crea un obiettivo.'}</p><div className="quick-row"><button onClick={()=>nav('focus')} className="primary">Inizia Focus<Zap/></button><button onClick={()=>nav('quiz')}>Quiz & Abilità<Sparkles/></button></div></Panel><Panel><div className="panel-head"><div><span className="eyebrow">SKILL TREE</span><h2>Mastery</h2></div><Sparkles/></div><div className="lp-skill-grid">{skills.length?skills.map(s=><article className="lp-skill" key={s.skill}><div className="lp-skill-head"><strong>{s.skill}</strong><b>{s.mastery}%</b></div><div className="lp-skill-bar"><i style={{width:`${s.mastery}%`}}/></div><small className="muted">{s.correct}/{s.attempts} corrette</small></article>):<p className="muted">Completa il primo quiz per iniziare la mastery.</p>}</div></Panel></div>}
function Progress({tasks,profile}){const completed=tasks.filter(t=>t.completed).length;return <div className="page"><Header title="Progressi" subtitle="Misura la costanza e la crescita reale."/><div className="metric-grid"><Metric icon={<Sparkles/>} label="XP" value={profile.xp||0}/><Metric icon={<Trophy/>} label="Livello" value={profile.level||1}/><Metric icon={<Flame/>} label="Streak" value={`${profile.current_streak||0} gg`}/><Metric icon={<Check/>} label="Completamento" value={`${pct(completed,tasks.length)}%`}/></div><Panel><div className="panel-head"><h2>Ultimi 7 giorni</h2><BarChart3/></div><div className="bars">{Array.from({length:7},(_,i)=>{const d=new Date();d.setDate(d.getDate()-6+i);const k=d.toISOString().slice(0,10);const n=tasks.filter(t=>t.task_date===k&&t.completed).length;return <div key={k}><div className="bar" style={{height:`${Math.max(8,Math.min(100,n*18))}%`}}/><span>{d.toLocaleDateString('it-IT',{weekday:'short'})}</span></div>})}</div></Panel></div>}
function Notifications({uid}){const [items,setItems]=useState([]);useEffect(()=>{supabase.from('notifications').select('*').eq('user_id',uid).order('created_at',{ascending:false}).limit(50).then(({data})=>setItems(data||[]))},[uid]);return <div className="page"><Header title="Notifiche" subtitle="Le cose importanti al momento giusto."/><Panel>{items.length?items.map(n=><button className={`notice ${n.read_at?'read':''}`} key={n.id} onClick={async()=>{const at=new Date().toISOString();await supabase.from('notifications').update({read_at:at}).eq('id',n.id).eq('user_id',uid);setItems(items.map(x=>x.id===n.id?{...x,read_at:at}:x))}}><Bell/><div><strong>{n.title}</strong><p>{n.body}</p></div></button>):<p className="muted">Nessuna notifica.</p>}</Panel></div>}
function Profile({profile,uid,reload}){const [name,setName]=useState(profile.nickname||''),[birth,setBirth]=useState(profile.birth_date||''),[msg,setMsg]=useState('');const save=async()=>{const {error}=await supabase.from('profiles').update({nickname:name.trim(),birth_date:birth}).eq('id',uid);setMsg(error?.message||'Profilo aggiornato.');if(!error)reload()};return <div className="page"><Header title="Profilo" subtitle="Il tuo percorso, sotto il tuo controllo."/><Panel><label>Nickname<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Data di nascita<input type="date" value={birth} onChange={e=>setBirth(e.target.value)}/></label>{msg&&<div className="msg ok">{msg}</div>}<button className="primary" onClick={save}>Salva modifiche<Check/></button></Panel></div>}
function TaskRow({task}){return <div className="task-row"><span className={task.completed?'check done':'check'}>{task.completed&&<Check/>}</span><div><strong className={task.completed?'strike':''}>{task.title}</strong><small>{task.task_date}</small></div></div>}
function Metric({icon,label,value}){return <div className="metric"><span>{icon}</span><div><small>{label}</small><strong>{value}</strong></div></div>}
function ProgressBar({value}){const v=Math.max(0,Math.min(100,Number(value)||0));return <div className="progress" role="progressbar" aria-valuenow={v} aria-valuemin="0" aria-valuemax="100"><i style={{width:`${v}%`}}/></div>}
function Header({title,subtitle,action}){return <div className="page-head"><div><span className="eyebrow">LIFEPILOT</span><h1>{title}</h1><p>{subtitle}</p></div>{action}</div>}
function Panel({children}){return <section className="panel">{children}</section>}
function Empty({title,text,action,onClick}){return <Panel><div className="empty"><Sparkles/><h2>{title}</h2><p>{text}</p><button className="primary" onClick={onClick}>{action}<ArrowRight/></button></div></Panel>}
function Modal({title,close,children}){return <div className="modal-bg"><div className="modal" role="dialog" aria-modal="true"><div className="panel-head"><h2>{title}</h2><button className="icon" onClick={close} aria-label="Chiudi"><X/></button></div>{children}</div></div>}
function Centered({children}){return <div className="center">{children}</div>}
